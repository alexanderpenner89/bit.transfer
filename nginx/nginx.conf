upstream ghost {
    server ghost:2368;
}

upstream devtools {
    server devtools:8000;
}

server {
    listen 80;
    server_name alexanderpenner.de www.alexanderpenner.de;

    # /api → FastAPI backend (used by devtools frontend)
    location /api/ {
        auth_basic "DevTools";
        auth_basic_user_file /etc/nginx/.htpasswd;

        proxy_pass http://devtools/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        # SSE support (for streaming endpoints)
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
    }

    # /devtools → FastAPI backend
    location /devtools/ {
        auth_basic "DevTools";
        auth_basic_user_file /etc/nginx/.htpasswd;

        proxy_pass http://devtools/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        # SSE support (for streaming endpoints)
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
    }

    # Everything else → Ghost blog
    location / {
        proxy_pass http://ghost;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
