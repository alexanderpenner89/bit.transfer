[project]
name = "gewerks-backend"
version = "0.1.0"
description = "Backend mit Pydantic-Schemas für Gewerks-Profile"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "mypy>=1.0.0",
]
